import React from 'react';

interface FooterProps{

};

const Footer: React.FC<FooterProps> = ({}) => {
    return (
        <div>
            Footer
        </div>
    )
}

export default Footer;